{
  "name": "@exchange/wallet-taker",
  "version": "1.0.0",
  "description": "High-availability wallet taker client for exchange platform",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "pino": "^8.17.2",
    "pino-pretty": "^13.1.3",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.5.10"
  }
}
